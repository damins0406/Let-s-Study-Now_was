name: Backend PR Auto Comment

on:
  pull_request:
    types: [opened]

jobs:
  comment:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: PR 자동 댓글
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `⚙️ 백엔드 PR이 생성되었습니다!

              ## 확인 사항
              - [ ] 테스트 코드 작성
              - [ ] API 문서 업데이트
              - [ ] 빌드 성공
              - [ ] 로컬 테스트 완료

              리뷰를 시작합니다! 💪`
            })