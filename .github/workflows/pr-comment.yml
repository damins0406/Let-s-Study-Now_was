name: Backend PR Auto Comment
on:
  pull_request_target:
    types: [opened]

jobs:
  comment:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: PR 자동 댓글
        uses: actions/github-script@v7
        with:
          script: |
            const body = "⚙️ 백엔드 PR이 생성되었습니다! \n\n" +
                         "## 확인 사항\n" +
                         "- [ ] 빌드 성공\n" +
                         "- [ ] 테스트 코드 작성 및 통과\n" +
                         "- [ ] API 문서 업데이트\n" +
                         "- [ ] 로컬 테스트 완료\n\n" +
                         "리뷰 시작하겠습니다! 🚀";

            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body
            });